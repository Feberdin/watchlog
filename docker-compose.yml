# Purpose: Public Docker Compose stack for normal self-hosted WatchLog installs.
# Input/Output: Reads `.env`, starts PostgreSQL and WatchLog, and exposes the web UI on APP_PORT.
# Invariants: No privileged containers, no host network, and no real secrets committed to git.
# Debugging: Use `docker compose logs -f watchlog` and `curl http://localhost:8111/readyz`.

x-watchlog-logging: &watchlog-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

x-broker:
  heimdall:
    enabled: true

services:
  db:
    image: postgres:16-alpine@sha256:16bc17c64a573ef34162af9298258d1aec548232985b33ed7b1eac33ba35c229
    container_name: watchlog-db
    restart: unless-stopped
    logging: *watchlog-logging
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-watchlog}
      POSTGRES_USER: ${POSTGRES_USER:-watchlog}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      TZ: ${TZ:-Europe/Berlin}
    volumes:
      - watchlog-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-watchlog} -d ${POSTGRES_DB:-watchlog}"]
      interval: 10s
      timeout: 5s
      retries: 10

  # Why this exists: existing named volumes may have been created by an older
  # root-running image. This one-shot helper fixes only volume ownership, drops
  # all unrelated capabilities, and exits before the API starts. It deliberately
  # reuses the application image so a deployment performs one expensive build.
  watchlog-permissions:
    image: watchlog-app:${WATCHLOG_IMAGE_TAG:-local}
    pull_policy: never
    user: "0:0"
    command: ["sh", "-c", "chown -R 1000:1000 /config /cache"]
    restart: "no"
    healthcheck:
      disable: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
    logging: *watchlog-logging
    volumes:
      - watchlog-config:/config
      - watchlog-cache:/cache

  watchlog:
    image: watchlog-app:${WATCHLOG_IMAGE_TAG:-local}
    build:
      context: .
      args:
        APP_COMMIT: ${APP_COMMIT:-unknown}
    pull_policy: build
    container_name: watchlog
    restart: unless-stopped
    logging: *watchlog-logging
    labels:
      unraid-deploy-broker.heimdall.enabled: "true"
      unraid-deploy-broker.heimdall.probe.enabled: "true"
      unraid-deploy-broker.heimdall.probe.path: "/api/heimdall/v1"
    depends_on:
      db:
        condition: service_healthy
      watchlog-permissions:
        condition: service_completed_successfully
    environment:
      APP_URL: ${APP_URL:-http://localhost:8111}
      APP_PORT: 8111
      TZ: ${TZ:-Europe/Berlin}
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL in .env}
      SESSION_SECRET: ${SESSION_SECRET:?Set SESSION_SECRET in .env}
      WEBHOOK_SECRET: ${WEBHOOK_SECRET:?Set WEBHOOK_SECRET in .env}
      JELLYFIN_URL: ${JELLYFIN_URL:-}
      JELLYFIN_API_KEY: ${JELLYFIN_API_KEY:-}
      JELLYSEERR_URL: ${JELLYSEERR_URL:-}
      JELLYSEERR_API_KEY: ${JELLYSEERR_API_KEY:-}
      TMDB_BEARER_TOKEN: ${TMDB_BEARER_TOKEN:-}
      REGISTRATION_ENABLED: ${REGISTRATION_ENABLED:-false}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      SECURE_COOKIES: ${SECURE_COOKIES:-false}
      CACHE_DIR: /cache
    ports:
      - "${APP_PORT:-8111}:8111"
    volumes:
      - watchlog-config:/config
      - watchlog-cache:/cache
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:8111/readyz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 5

volumes:
  watchlog-db:
  watchlog-config:
  watchlog-cache:
